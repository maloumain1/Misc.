pageextension 50103 "AR Customer Ext" extends "Finance A/R Customer list"
{
    actions
    {
        addlast(Processing)
        {
            action(OpenNewCLE)
            {
                Caption = 'Customer Ledger Entries';
                ApplicationArea = All;
                Image = LedgerEntries;

                trigger OnAction()
                begin
                    Page.Run(Page::"New Customer Ledger Entries");
                end;
            }
        }
    }
}
